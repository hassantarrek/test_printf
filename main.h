#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

/**
 * header file for printf functions
 * adding decelration of printf functions
 */

int _printf(const char *format, ...);
int _strlen(char *s);
int print_int(int n);

#endif
